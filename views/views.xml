<data>
  <!--<template id="chatter.view_journal_form_chatter" inherit_id="account.view_account_journal_form" >
    <xpath expr="//sheet" position="after">
      <div class="oe_chatter">
        <field name="message_follower_ids" widget="mail_followers" groups="base.group_user" />
        <field name="activity_ids" widget="mail_activity" />
        <field name="message_ids" widget="mail_thread" />
      </div>
    </xpath> 
  </template>-->
  
  <record id="chatter.view_journal_form_chatter" model="ir.ui.view">
    <field name="name">account.journal.form.chatter</field>
    <field name="model">account.journal</field>
    <field name="inherit_id" ref="account.view_account_journal_form" />
    <field name="arch" type="xml">
      <xpath expr="//sheet" position="after">
        <div class="oe_chatter">
          <field name="message_follower_ids" widget="mail_followers" groups="base.group_user" />
          <field name="activity_ids" widget="mail_activity" />
          <field name="message_ids" widget="mail_thread" />
        </div>
      </xpath> 
    </field>
  </record>

  <!-- explicit list view definition -->
  <!--
    <record model="ir.ui.view" id="chatter.list">
      <field name="name">chatter list</field>
      <field name="model">chatter.chatter</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

  <!-- actions opening views on models -->
  <!--
    <record model="ir.actions.act_window" id="chatter.action_window">
      <field name="name">chatter window</field>
      <field name="res_model">chatter.chatter</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

  <!-- server action to the one above -->
  <!--
    <record model="ir.actions.server" id="chatter.action_server">
      <field name="name">chatter server</field>
      <field name="model_id" ref="model_chatter_chatter"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

  <!-- Top menu item -->
  <!--
    <menuitem name="chatter" id="chatter.menu_root"/>
    -->
  <!-- menu categories -->
  <!--
    <menuitem name="Menu 1" id="chatter.menu_1" parent="chatter.menu_root"/>
    <menuitem name="Menu 2" id="chatter.menu_2" parent="chatter.menu_root"/>
    -->
  <!-- actions -->
  <!--
    <menuitem name="List" id="chatter.menu_1_list" parent="chatter.menu_1"
              action="chatter.action_window"/>
    <menuitem name="Server to list" id="chatter" parent="chatter.menu_2"
              action="chatter.action_server"/>
    -->
</data>